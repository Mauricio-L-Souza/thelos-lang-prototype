class ShuttingYard {
    __operators = [];
    __output = [];

    execute(input) {
        let cursor = 0;
        while (cursor < input.length) {
            // ALL different of operand must be in __output array
        }
    }

    __parentize() {

    }
}

module.exports = ShuttingYard;
